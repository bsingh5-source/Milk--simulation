<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Milk Mystery Game</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Lexend:wght@300;400;700&display=swap');
        body { font-family: 'Lexend', sans-serif; }
        .person { transition: all 0.5s ease; }
        .allele-L { color: #3b82f6; } /* Blue for L */
        .allele-l { color: #ef4444; } /* Red for l */
        .dna-card { border: 2px solid #e2e8f0; border-radius: 8px; padding: 4px; text-align: center; }
    </style>
</head>
<body class="bg-slate-100 min-h-screen pb-12">
    <div class="max-w-4xl mx-auto p-4">
        <!-- Header -->
        <header class="bg-white rounded-2xl shadow-sm p-6 mb-6 mt-4 border-b-4 border-blue-500">
            <div class="flex justify-between items-center">
                <div>
                    <h1 class="text-2xl font-bold text-slate-800">🥛 Milk Mystery: Simulation</h1>
                    <p class="text-slate-500">Watch how DNA changes over 5 generations!</p>
                </div>
                <div class="text-right">
                    <span id="gen-display" class="bg-blue-100 text-blue-700 px-4 py-2 rounded-full font-bold">Generation: 1</span>
                </div>
            </div>
        </header>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
            <!-- Sidebar: Controls -->
            <div class="space-y-4">
                <div class="bg-white p-6 rounded-2xl shadow-sm border border-slate-200">
                    <h2 class="font-bold text-lg mb-4">1. Choose Mode</h2>
                    <div class="space-y-2">
                        <button onclick="setMode('none')" id="mode-none" class="w-full py-3 px-4 rounded-xl border-2 border-blue-500 bg-blue-50 text-blue-700 font-bold flex items-center justify-between">
                            <span>No Pressure</span>
                            <i class="fas fa-bread-slice"></i>
                        </button>
                        <button onclick="setMode('famine')" id="mode-famine" class="w-full py-3 px-4 rounded-xl border-2 border-slate-200 hover:border-blue-300 text-slate-600 font-bold flex items-center justify-between">
                            <span>Famine (Only Milk)</span>
                            <i class="fas fa-skull-crossbones"></i>
                        </button>
                    </div>
                    <p class="text-xs text-slate-400 mt-3 italic" id="mode-desc">Simulation 1: Everyone survives regardless of DNA.</p>
                </div>

                <div class="bg-white p-6 rounded-2xl shadow-sm border border-slate-200">
                    <h2 class="font-bold text-lg mb-2">2. Play</h2>
                    <button onclick="nextGeneration()" id="btn-next" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-bold py-4 rounded-xl shadow-lg shadow-blue-200 transition-all flex items-center justify-center gap-2">
                        <i class="fas fa-dna"></i> Next Generation
                    </button>
                    <button onclick="resetGame()" class="w-full mt-2 text-slate-400 text-sm hover:text-slate-600">Reset to Gen 1</button>
                </div>

                <!-- Stats -->
                <div class="bg-slate-800 text-white p-6 rounded-2xl shadow-sm">
                    <h2 class="font-bold text-sm uppercase tracking-widest opacity-70 mb-4">Allele Frequency</h2>
                    <div class="space-y-4">
                        <div>
                            <div class="flex justify-between text-xs mb-1">
                                <span>L (Tolerant)</span>
                                <span id="stat-L-text">50%</span>
                            </div>
                            <div class="w-full bg-slate-700 rounded-full h-2">
                                <div id="bar-L" class="bg-blue-400 h-2 rounded-full transition-all duration-500" style="width: 50%"></div>
                            </div>
                        </div>
                        <div>
                            <div class="flex justify-between text-xs mb-1">
                                <span>l (Intolerant)</span>
                                <span id="stat-l-text">50%</span>
                            </div>
                            <div class="w-full bg-slate-700 rounded-full h-2">
                                <div id="bar-l" class="bg-red-400 h-2 rounded-full transition-all duration-500" style="width: 50%"></div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Main: Population Grid -->
            <div class="md:col-span-2 space-y-4">
                <div class="bg-white p-6 rounded-2xl shadow-sm border border-slate-200 min-h-[400px]">
                    <div class="flex justify-between items-center mb-6">
                        <h2 class="font-bold text-xl">The Village</h2>
                        <div class="flex gap-4 text-xs font-bold">
                            <span class="flex items-center gap-1"><i class="fas fa-user text-blue-500"></i> LL/Ll (Healthy)</span>
                            <span class="flex items-center gap-1"><i class="fas fa-user text-red-500"></i> ll (Sick)</span>
                        </div>
                    </div>
                    
                    <div id="grid" class="grid grid-cols-5 sm:grid-cols-10 gap-4">
                        <!-- People generated by JS -->
                    </div>

                    <div id="famine-overlay" class="hidden mt-8 p-4 bg-red-50 border border-red-200 rounded-xl flex items-center gap-4 animate-pulse">
                        <i class="fas fa-exclamation-triangle text-red-500 text-2xl"></i>
                        <div>
                            <p class="font-bold text-red-800">Famine Active!</p>
                            <p class="text-xs text-red-600">Only Milk Drinkers (L) are surviving to reproduce.</p>
                        </div>
                    </div>
                </div>

                <!-- History Chart -->
                <div class="bg-white p-6 rounded-2xl shadow-sm border border-slate-200">
                    <h3 class="font-bold mb-4">Genetics Timeline</h3>
                    <div id="timeline" class="flex items-end gap-2 h-24 border-b border-slate-100">
                        <!-- History bars -->
                    </div>
                    <div class="flex justify-between text-[10px] text-slate-400 mt-2">
                        <span>Start</span>
                        <span>Gen 5</span>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        let generation = 1;
        let population = [];
        let mode = 'none'; // 'none' or 'famine'
        let history = [];

        // Initial Population: Everyone is Ll (50% L, 50% l)
        function initPopulation() {
            population = [];
            for (let i = 0; i < 40; i++) {
                population.push({ a1: 'L', a2: 'l' });
            }
            updateUI();
        }

        function setMode(m) {
            mode = m;
            document.getElementById('mode-none').className = m === 'none' ? 'w-full py-3 px-4 rounded-xl border-2 border-blue-500 bg-blue-50 text-blue-700 font-bold flex items-center justify-between' : 'w-full py-3 px-4 rounded-xl border-2 border-slate-200 hover:border-blue-300 text-slate-600 font-bold flex items-center justify-between';
            document.getElementById('mode-famine').className = m === 'famine' ? 'w-full py-3 px-4 rounded-xl border-2 border-red-500 bg-red-50 text-red-700 font-bold flex items-center justify-between' : 'w-full py-3 px-4 rounded-xl border-2 border-slate-200 hover:border-blue-300 text-slate-600 font-bold flex items-center justify-between';
            document.getElementById('mode-desc').innerText = m === 'none' ? 'Simulation 1: Everyone survives regardless of DNA.' : 'Simulation 2: People with "ll" DNA cannot survive famine.';
            document.getElementById('famine-overlay').classList.toggle('hidden', m !== 'famine');
            resetGame();
        }

        function nextGeneration() {
            if (generation >= 5) {
                alert("Five generations complete! Look at your final results.");
                return;
            }

            let parents = [...population];
            
            // In Famine mode, "ll" people don't get to be parents
            if (mode === 'famine') {
                parents = population.filter(p => p.a1 === 'L' || p.a2 === 'L');
                if (parents.length < 2) {
                    alert("Population too small to continue!");
                    return;
                }
            }

            let nextPop = [];
            for (let i = 0; i < 40; i++) {
                // Randomly pick two parents
                const p1 = parents[Math.floor(Math.random() * parents.length)];
                const p2 = parents[Math.floor(Math.random() * parents.length)];
                
                // Inherit alleles (50/50 chance from each parent)
                const a1 = Math.random() > 0.5 ? p1.a1 : p1.a2;
                const a2 = Math.random() > 0.5 ? p2.a1 : p2.a2;
                
                nextPop.push({ a1, a2 });
            }

            population = nextPop;
            generation++;
            updateUI();
        }

        function updateUI() {
            const grid = document.getElementById('grid');
            grid.innerHTML = '';
            
            let countL = 0;
            let countTotal = population.length * 2;

            population.forEach(p => {
                const genotype = p.a1 + p.a2;
                const isSick = genotype === 'll';
                if (p.a1 === 'L') countL++;
                if (p.a2 === 'L') countL++;

                const div = document.createElement('div');
                div.className = 'person flex flex-col items-center animate-bounce';
                div.style.animationIterationCount = '1';
                div.innerHTML = `
                    <i class="fas fa-user text-2xl ${isSick ? 'text-red-400' : 'text-blue-500'}"></i>
                    <div class="text-[10px] font-bold mt-1">
                        <span class="allele-${p.a1}">${p.a1}</span><span class="allele-${p.a2}">${p.a2}</span>
                    </div>
                `;
                grid.appendChild(div);
            });

            const percentL = Math.round((countL / countTotal) * 100);
            const percentSmallL = 100 - percentL;

            document.getElementById('stat-L-text').innerText = percentL + '%';
            document.getElementById('stat-l-text').innerText = percentSmallL + '%';
            document.getElementById('bar-L').style.width = percentL + '%';
            document.getElementById('bar-l').style.width = percentSmallL + '%';
            document.getElementById('gen-display').innerText = `Generation: ${generation}`;

            // Add to timeline
            const timeline = document.getElementById('timeline');
            const bar = document.createElement('div');
            bar.className = 'flex-1 bg-blue-400 rounded-t';
            bar.style.height = `${percentL}%`;
            timeline.appendChild(bar);

            if (generation === 5) {
                document.getElementById('btn-next').innerText = "Simulation Complete";
                document.getElementById('btn-next').className = "w-full bg-green-600 text-white font-bold py-4 rounded-xl shadow-lg transition-all flex items-center justify-center gap-2";
            }
        }

        function resetGame() {
            generation = 1;
            document.getElementById('timeline').innerHTML = '';
            document.getElementById('btn-next').innerText = "Next Generation";
            document.getElementById('btn-next').className = "w-full bg-blue-600 hover:bg-blue-700 text-white font-bold py-4 rounded-xl shadow-lg shadow-blue-200 transition-all flex items-center justify-center gap-2";
            initPopulation();
        }

        window.onload = initPopulation;
    </script>
</body>
</html>
